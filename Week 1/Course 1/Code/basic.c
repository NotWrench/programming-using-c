// Header files - this is where you import your libraries and stuff
#include <stdio.h> // This imports the standard input/output library in C

// This is called main function. Every C file need to have a main function
/*
Syntax for main function:
int main()
{
    // statement(s)
}
*/
int main() {
  // Outputs the given string to the standard output
  printf("Hello World!");

  // If the main function returns 0, it means the code executed without errors
  // If 1 it means the code executed with errors
  return 0;
}
